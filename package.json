{
  "name": "leadsite",
  "version": "3.0.0",
  "description": "Prospecção → CRM → criação de sites para empresas locais",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node build.js",
    "previa": "node build-previa.js",
    "check": "node build.js --check && node build-previa.js --check",
    "test": "node verificar.js && node testar-navegador.js",
    "test:rapido": "node verificar.js --rapido",
    "test:servidor": "node verificar.js",
    "test:navegador": "node testar-navegador.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
