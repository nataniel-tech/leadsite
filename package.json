{
  "name": "leadsite",
  "version": "3.0.0",
  "description": "Prospecção → CRM → criação de sites para empresas locais",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node scripts/build.js",
    "previa": "node scripts/build-previa.js",
    "check": "node scripts/build.js --check && node scripts/build-previa.js --check",
    "test": "node scripts/verificar.js && node scripts/testar-navegador.js",
    "test:rapido": "node scripts/verificar.js --rapido",
    "test:servidor": "node scripts/verificar.js",
    "test:navegador": "node scripts/testar-navegador.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
