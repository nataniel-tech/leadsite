# dados de runtime (leads, fotos enviadas)
data/db.json
data/fotos/
data/backups/

# dependências
node_modules/

# ambiente e segredos — nunca versionar token ou senha
.env
.env.*
*.pem

# backups e temporários
*.tmp
.tmp-*
*.log
.DS_Store
